# PROJECT-STATE

Live state of the managed marketplace project `Marketplace-Ve-Xe-Nhanh` (codename renamed 25/05/2026 from `Ve_Xe_Nhanh_NestJS_NextJs_ReactNative`). Agents must consult this file before writing or editing any SDLC document. Update this file after every documentation change that affects status, decisions or blockers.

Last updated: 25/09/2026.

## 1. SDLC document status

> **Version**: bỏ số `Phiên bản` (02/06/2026) — git là nguồn lịch sử. Mỗi doc giữ **Trạng thái** + **Ngày cập nhật**; lịch sử thay đổi xem `git log` + `archive/CHANGELOG.md`.

| Code | Document                                  | Path                                       | Status   | Owner                    |
| ---- | ----------------------------------------- | ------------------------------------------ | -------- | ------------------------ |
| 00a  | Quy chuẩn SDLC cho lập trình viên         | `SDLC/00-quy-chuan-cho-lap-trinh-vien.md`  | Approved | Nguyễn Hồng Khanh        |
| 01   | SRS - Software Requirements Specification | `SDLC/01-srs-he-thong-dat-ve-xe-khach.md`  | Approved | Nguyễn Hồng Khanh + team |
| 02   | HLD - High Level Design                   | `SDLC/02-hld-he-thong-dat-ve-xe-khach.md`  | Approved | AI Agent                 |
| 03   | LLD - Low Level Design                    | `SDLC/03-lld-he-thong-dat-ve-xe-khach.md`  | Approved | AI Agent                 |
| 04   | Database Design                           | `SDLC/04-database-design.md`               | Approved | AI Agent                 |
| 05   | API Specification                         | `SDLC/05-api-specification.md`             | Approved | AI Agent                 |
| 06   | UI / UX Flow Specification                | `SDLC/06-ui-ux-flow-specification.md`      | Review   | AI Agent                 |
| 07   | Security & Permission Design              | `SDLC/07-security-permission-design.md`    | Approved | AI Agent                 |
| 08   | Test Plan & Acceptance Criteria           | `SDLC/08-test-plan-acceptance-criteria.md` | Draft    | AI Agent                 |
| 09   | Deployment & Operation Standard           | `SDLC/09-deployment-operation-standard.md` | Approved | AI Agent                 |
| 10   | Architecture Decision Record              | `SDLC/10-architecture-decision-record.md`  | Approved | AI Agent                 |
| 11   | Project Task Breakdown                    | `SDLC/11-project-task-breakdown.md`        | Approved | AI Agent                 |
| 12   | Release Notes & Change Log                | `SDLC/12-release-notes-change-log.md`      | Draft    | AI Agent                 |

Status legend per `00a §3.2`: `Writing` / `Draft` / `Review` / `Approved` / `Deprecated` / `Superseded`.

## 2. Strategic decisions

Position: **fresh build under codename `Marketplace-Ve-Xe-Nhanh`** (renamed 25/05/2026 from `Ve_Xe_Nhanh_NestJS_NextJs_ReactNative`).

- Original decision "Phương án A — full rewrite" (11/05/2026) was the path to this position; the 25/05/2026 rebrand formalised it: no migration from legacy, no "skeleton" considered.
- Any files left under `apps/backend/src/modules/` from the prior codebase are **out of scope** for v1 and must not be treated as a reference when designing or coding.
- Target backend layout follows three service layers — see `@context/DOMAIN-MAP §1, §2`.
- `DOMAIN-MAP` describes target state only; no refactor radar / legacy-to-target mapping is maintained.
- **Tech stack đang re-select toàn bộ** (decided 25/05/2026): the stack referenced in v0.3 SDLC docs (NestJS/Next/Expo/Mongo/Redis/OSRM) was carryover from the legacy codebase, not a deliberate choice for the marketplace project. Workshop 15 layer / 4 phase per `CLAUDE.md §6.5`. 5 vendor OQs (OQ-05/09/10/14/16) re-opened — see §4. Several v0.3 SDLC docs are held back from Review pending tech selection — see §5.

## 3. Closed decisions log (OQ + MQ)

25 OQ/MQ từ SRS đã đóng 11/05/2026. 5 vendor OQ (OQ-05/09/10/14/16) re-opened 25/05/2026 (full tech re-select) → **tất cả re-closed** qua ADR. Chi tiết rationale: file 10 ADR + `archive/CHANGELOG.md`. SRS §21 có full text gốc.

| ID        | Topic                                                         | Status                                                                                                                                  |
| --------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| OQ-01..03 | State enums (Trip/Booking/Payment) align SRS §17              | Closed 05/05                                                                                                                            |
| OQ-04     | Employee roles TICKET_STAFF/DRIVER/SUPPORT_STAFF              | Closed 05/05                                                                                                                            |
| OQ-05     | Payment gateway                                               | Re-closed → **ADR-019** (VNPay primary + MoMo)                                                                                          |
| OQ-06     | Seat hold 10 phút platform-wide                               | Closed 11/05                                                                                                                            |
| OQ-07     | Pay-first only; PENDING_CONFIRMATION giữ enum                 | Closed 11/05                                                                                                                            |
| OQ-08     | Fare collection riêng + booking snapshot; no segment fare     | Closed 11/05                                                                                                                            |
| OQ-09     | Notification + OTP                                            | Re-closed → **ADR-020** (Resend + push + OAuth); push đổi sang **FCM + APNs trực tiếp** theo **ADR-028** (08/09/2026)                   |
| OQ-10     | Mobile architecture                                           | Re-closed lần 2 → **ADR-028** (08/09/2026, **Flutter** 2-app split, lý do = phục vụ môn học); ADR-014 (Expo 2-app split) **Superseded** |
| OQ-11     | Phone mask `0*** *** 789`                                     | Closed 11/05                                                                                                                            |
| OQ-12     | VND + Vietnamese only v1                                      | Closed 11/05                                                                                                                            |
| OQ-13     | Cancel/refund Platform default + Operator override + snapshot | Closed 11/05                                                                                                                            |
| OQ-14     | Audit log storage                                             | Re-closed → **ADR-011** (Mongo cluster RIÊNG)                                                                                           |
| OQ-15     | Reporting aggregation + async                                 | Closed 11/05 (refined ADR-011)                                                                                                          |
| OQ-16     | Escrow payout channel                                         | Re-closed → **ADR-022** (manual + batch)                                                                                                |
| OQ-17     | Price ceiling/floor = warning only                            | Closed 10/05                                                                                                                            |
| OQ-18     | Commission 5% default + per-Operator override                 | Closed 11/05                                                                                                                            |
| OQ-19     | KYC = biz reg + transport license + rep ID + bank             | Closed 11/05                                                                                                                            |
| OQ-20     | Brand neutral marketplace; operator name primary              | Closed 11/05                                                                                                                            |
| MQ-01..05 | Marketplace model (escrow/pricing/arbiter/commission/model B) | Closed 05/05                                                                                                                            |

## 4. Open Questions (current)

5 vendor OQ re-opened 25/05/2026 đã **tất cả re-closed** (xem §3 + ADR). Còn **2 OQ mở** — production-blocker, KHÔNG chặn MVP/sandbox:

| ID    | Question                                                                                                                                                                                                                                                                       | Impact                        | Status                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------- | -------------------------------------------- |
| OQ-21 | KYC + payment-proof production storage location (VN local split-bucket vs R2 + DPIA NĐ 13/2023 cross-border); dev/test = local adapter                                                                                                                                         | Compliance + KYC + storage    | Open — defer production prep (ADR-018 Q2)    |
| OQ-22 | Giấy phép TGTT NHNN production: Platform giữ escrow + chi Operator = trung gian thanh toán (NĐ 52/2024) → cần license NHNN HOẶC qua cổng licensed. Hướng (a) tự xin TGTT; (b) cổng licensed VNPay/MoMo giữ+settle, escrow = sổ kế toán; (c) thu hộ/chi hộ qua đối tác licensed | Compliance + Payment + Escrow | Open — defer production prep (ADR-019 mục C) |

## 5. Blockers

Concrete blockers preventing a document from reaching `Review` or `Approved`. Updated 25/05/2026 — full tech re-select pause.

| Document                                  | Blocker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Owner      | Since      |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------- |
| 02 HLD §13 (tech stack), §8 (module map)  | **Resolved 01/06/2026** — Sprint 5 rework: HLD reset v0.4 theo 18 ADR; sẵn sàng Khanh review → Review. 1 OQ phụ HLD-OQ-11 (realtime transport) có fallback polling, không chặn normative.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Khanh + AI | 25/05/2026 |
| 03 LLD §5 (module structure)              | **Resolved 01/06/2026** — Sprint 5 rework v0.4 theo 18 ADR; **ADR-017 re-confirmed by Khanh** (chốt toàn bộ); thêm 6.5 auth + 6.6 payout; đóng LLD-OQ-01/05. Sẵn sàng Khanh review → Review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Khanh + AI | 25/05/2026 |
| 04 DB Design (toàn file)                  | **Resolved 01/06/2026** — Sprint 5 rework: reset v0.4 theo ADR-011 Hybrid-A (Postgres+Prisma ops / Mongo audit cluster riêng); đóng DB-OQ-01/04/05; sẵn sàng Khanh review → Review. Còn DB-OQ-03 (escrow accounting model, LLD-level) + DB-OQ-06 (retention) mở, không chặn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Khanh + AI | 25/05/2026 |
| 05 API Spec                               | **Resolved 01/06/2026** — Sprint 5 rework v0.2 theo ADR-012 (REST + OpenAPI 3.1 auto/Zod, RFC 7807, webhook HMAC VNPay/MoMo, auth 3-namespace); đóng API-OQ-01/02/03; sẵn sàng Khanh review → Review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Khanh + AI | 25/05/2026 |
| 07 Security §5 (auth), §6 (tenant filter) | **Unblocked 26/05/2026** (ADR-017 chốt Better Auth + Identity model 3 namespace `{slug}/{username}` + Account separate + Closed enrollment Platform-controlled + Hybrid JWT+opaque token + RBAC 8 role + TenantGuard JWT claims + Postgres RLS defense-in-depth; ADR-018 chốt storage R2 + KYC private bucket presigned URL + audit access, KYC production location defer OQ-21) — chờ Khanh quyết khi nào rework. **CRITICAL re-confirm Sprint 5-6** do Khanh "[No preference]" vòng 1 + reframe vòng 2. **ADR-017 re-confirmed 01/06/2026** (Khanh chốt toàn bộ qua AskUserQuestion khi rework 03 LLD); **ADR-019/020 re-confirmed 01/06/2026** (Khanh chốt VNPay primary + ADR-020 toàn bộ qua AskUserQuestion). **Resolved 01/06/2026** — 07 Security rework v0.4 (Better Auth 3-namespace + Hybrid token + MFA + OAuth + RLS + PCI SAQ-A + KYC R2); đóng SEC-OQ-01/02/04/06; sẵn sàng Khanh review → Review. | Khanh + AI | 26/05/2026 |
| 09 Deploy (toàn file)                     | **Resolved 01/06/2026** — reworked v0.4 theo ADR-023..026 (Render/worker tách/Prisma migration/Sentry+GitHub Actions); đóng OPS-OQ-01/02/03/05; sẵn sàng Khanh review → Review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Khanh + AI | 25/05/2026 |
| 10 ADR                                    | Active growth — **Sprint 0 + 1 + 2 + 3 CLOSED** (ADR-002 refined + ADR-009..022 added; ADR-003/004/007 superseded). Sprint 3 Phase 3 VN vendor 4/4 layer chốt 31/05–01/06/2026 (ADR-019 Payment / ADR-020 Notification+OAuth / ADR-021 Routing / ADR-022 Payout; re-close OQ-05/09/16). Toàn bộ 5 OQ vendor re-closed. **Sprint 4 Phase 4 DevOps CLOSED 01/06/2026** (ADR-023 Render / ADR-024 worker tách / ADR-025 test / ADR-026 CI-CD+Sentry). **19/19 layer roadmap (15 tech + 4 DevOps) CHỐT XONG** (ADR-002 + ADR-009..027; routing ADR-021→ADR-027 Goong, 02/06/2026).                                                                                                                                                                                                                                                                                                                                    | AI         | 01/06/2026 |
| 11 Task Breakdown                         | **Resolved 01/06/2026** — reworked v0.3 (task gắn 19-layer stack: Turborepo/Render/Prisma/BullMQ/Better Auth/VNPay-MoMo/Vitest); sẵn sàng Khanh review → Review.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Khanh + AI | 25/05/2026 |

Doc không bị block (vẫn có thể advance): 00 chuẩn, 01 SRS (business logic), 06 UI flow, 08 Test logic, GLOSSARY, DOMAIN-MAP (target naming layer, có thể cần update sau khi framework chốt).

## 6. Known doc / structure issues to clean up

| Issue                                                                    | Action proposed                                   | Status                                                                                                                              |
| ------------------------------------------------------------------------ | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| SDLC docs 03-LLD, 04-DB, 07-Security, 09-Deploy lack marketplace context | Review v0.1 drafts and align with SRS v1.15       | Resolved 25/05/2026 — bumped to v0.2; module list aligned with DOMAIN-MAP; closed OQs reflected; new OQs surfaced where gaps remain |
| HLD v0.1 was written before `DOMAIN-MAP` existed                         | Review HLD against new `DOMAIN-MAP` and SRS v1.15 | Resolved 25/05/2026 — HLD bumped to v0.2; §8.1 module table rewritten to target state; §13/§16/§18 updated with closed OQs          |

## 7. Recent change log (latest first)

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Author   |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| 25/09/2026 | **Đóng `TASK-OQ-05`; `TASK-IAM-006` Draft → Ready.** Contract web auth đã chốt: web opt-in `X-Auth-Transport: cookie`, Mobile mặc định giữ JSON/Bearer; host-only `vxn_access`/`vxn_refresh`; signed double-submit `vxn_csrf`; exact credentialed CORS + same-site domain; thêm `GET /auth/csrf` và `GET /auth/me`; Bearer+cookie đồng thời bị từ chối. Quyết định supersede phần Web defer của IAM-002 ngày 16/09 nhưng không đổi contract Mobile hay ADR-017. Đồng bộ LLD/API/UI/Security/Test/Deploy/Task + GLOSSARY; không promote trạng thái tài liệu. | AI Agent |
| 25/09/2026 | **Mở rộng 11 Task: thêm `TASK-IAM-006` cho web auth Operator OS + Platform Admin.** Khoảng trống được xác nhận: IAM-002 hiện trả token JSON cho Mobile; phần web cookie httpOnly + CSRF đã defer trong 07 Security §5.1, chưa có task sở hữu UI/state machine first-login/TOTP/session bootstrap/CORS. Task mới ở `Draft`, phụ thuộc FND-010 + IAM-004/005 và là dependency của `TASK-TRN-001` (màn Xe). `TASK-OQ-05` giữ các contract cookie/CSRF/CORS phải chốt trước khi code; không đổi ADR-017 hay contract Mobile. | AI Agent |
| 25/09/2026 | **TASK-FND-010 Done — CI xanh, PR #4 đã merge vào `develop`.** Nền FE web gồm Tailwind CSS 4, token VXN, Shadcn/ui dùng chung trong `packages/ui`, font Be Vietnam Pro và khung layout cho Marketplace/Operator OS/Admin. Khanh xác nhận CI xanh; kiểm tra lại local: lint + typecheck + test 12/12, `packages/ui` 3/3 test, production build 3/3 app. Checklist FND-010 và task row §7.1 đã đóng. | AI Agent |
| 25/09/2026 | **Tách §7.3 Transport resource trong 11 Task (v0.11)**: 4 → 9 task. Thêm `TASK-CAT-001` (Catalog nền: schema + seed DB-MIG-04 + API đọc; Admin CRUD/duyệt StopPoint vẫn ở ADM-001), `TRN-005` Fare, `TRN-006` vòng đời bán + khóa ghế thủ công, `TRN-007` lịch lặp lại, `TRN-008` đổi chuyến đã bán vé (chờ BTP-005 + NSR-001). Thu hẹp TRN-003 (Trip/TripStop/TripSeat); TRN-004 thêm chi tiết chuyến FR-MKT-05; TRN-002 bỏ phụ thuộc TRN-001; BTP-001 phụ thuộc TRN-006. Gỡ phụ thuộc vòng TRN-003 ↔ BTP. ⚠️ Cần Khanh duyệt endpoint mới khi làm task: API đọc catalog, đề xuất StopPoint, lịch lặp lại. | AI Agent |
| 25/09/2026 | **TASK-IAM-005 Done — CI 5/5 job xanh, PR #10 merge vào `develop`.** Rollout production 24/09: preflight slug Supabase 0 Owner/0 lệch → migrate 2 migration IAM-005 + `db:app-role` → Render live, kiểm tra RLS lúc khởi động qua, health 200. Chưa smoke nghiệp vụ trên production (chưa có Owner; provisioning chờ OPR-001). Checklist/todo IAM-005 và task row §7.2 đã đóng. | AI Agent |
| 24/09/2026 | **IAM-005 hardening local hoàn tất, chưa đóng task.** Đã tách `credentialDeliveryPending` khỏi trạng thái kỷ luật, bind challenge với `authEpoch` + trạng thái account/tenant, reserve quota Redis trước scrypt, che secret, dùng `version` riêng; mọi status transition kể cả unlock rotate epoch/revoke session. Evidence role app + PostgreSQL/Redis/Mongo thật: migration 7/7 idempotent, 48/48 file và 433/433 test không skip; typecheck/lint/build xanh; TS/OpenAPI regen sạch; Dart 150/150. Review sau fix không còn finding blocking/high/medium. IAM-005 vẫn Draft chờ smoke và CI. | AI Agent |
| 23/09/2026 | **IAM-005 hardening đang kiểm chứng, chưa đóng task.** Khanh xác nhận giữ Q8 recent re-auth bằng mật khẩu hoặc MFA, chưa bắt buộc TOTP riêng; duyệt quota email mật khẩu tạm 30/ngày global, 10/ngày/tenant, 5/ngày/actor và reset Employee cách 1 giờ. Đã sửa tài liệu API 05 v0.5, task breakdown 11 v0.9: `TASK-DOC-006` trở về Ready vì 08 Test vẫn Draft, `TASK-DOC-008` giữ Done vì chỉ yêu cầu tạo 12 Release Notes. 05/11 vẫn Approved, 08/12 vẫn Draft; IAM-005 vẫn Draft chờ DB smoke, review và CI. | AI Agent |
| 22/09/2026 | **TASK-IAM-005 triển khai local, chưa đóng gate.** Đã thêm migration/registry/RLS, temp-password và one-time change, Owner provisioning primitive + retry delivery, Employee lifecycle, session family API, re-auth/audit, OpenAPI + TS/Dart clients. Khóa/role/reset tăng `auth_epoch`; guard so account/session epoch trên mỗi request nội bộ để token cũ không sống lại nếu Redis post-revoke lỗi. Bằng chứng local: migration 6/6 trên Postgres test rỗng và upgrade SQL từ IAM-004 có 3 account legacy (backfill/registry/RLS đúng), diff không drift, role app idempotent, `REQUIRE_DB_TESTS=1` với Postgres/Redis/Mongo thật 386/386 pass không skip gồm provisioning/Employee service; turbo 35/35, TS client regen, Dart 148/148. Chưa có CI branch và smoke production; `dart analyze` còn 7 warning generated. Giữ task row `Draft`, không đổi status Approved SDLC. | AI Agent |
| 22/09/2026 | **TASK-IAM-005 `.1` chốt Q1–Q8 theo khuyến nghị, bắt đầu triển khai.** Khanh duyệt session family API, provisioning primitive, temp password 24h + one-time change challenge 5 phút, Employee lifecycle, registry namespace/slug invariant, không hard-cap session và recent re-auth/audit policy. Giữ defer KYC workflow, Platform employee, MFA reset và UI. Task row ở `Draft` cho tới khi test/review/smoke/CI đạt gate; không thay đổi trạng thái Approved của SDLC. | AI Agent |
| 21/09/2026 | **TASK-IAM-004 Done — CI branch xanh.** Khanh xác nhận CI hoàn tất; checklist/todo IAM-004 và task row §7.2 đã đóng sau khi toàn bộ test, smoke, review và CI đạt gate. **TASK-IAM-005 `.1` đã được phân tích và tạo todo/guide/checklist; đang chờ Khanh chốt Q1–Q8** vì task row yêu cầu duyệt endpoint session mới và ADR-017 còn thiếu contract temp-password/first-login trong schema/API. Không thay đổi trạng thái Approved của tài liệu SDLC. | AI Agent |
| 19/09/2026 | **TASK-IAM-004 `.1`–`.9` code xong, chờ Khanh commit + CI.** MFA TOTP bắt buộc cho `OPERATOR_OWNER` / `PLATFORM_ADMIN` / `PLATFORM_SUPPORT` theo Q1–Q10 (Khanh chốt 18/09): password đúng → **pre-auth challenge Redis 5 phút** (chưa token); chỉ `POST /auth/mfa/verify` cấp token; enrollment lần đầu qua cùng endpoint (`otpAuthUri` + đúng 10 backup code hiện một lần); RFC 6238 SHA-1/6/30s/±1 + chống dùng lại cùng time-step (compare-and-set `last_totp_counter`); secret AES-256-GCM (`MFA_ENCRYPTION_KEY`, production bắt buộc); backup code chỉ lưu hash, single-use atomic; re-auth nhận `password`/`otp`/`mfaCode`; token role bắt buộc phải có claim `mfa`, phiên cũ refresh → 401 `AUTH_MFA_REQUIRED`. Bảng `mfa_credentials` + `mfa_backup_codes` có **RLS chỉ `system`** (DB v0.7), API §7.1 làm rõ (v0.3). Review 2 agent: không đường nào cấp token thiếu MFA; đã sửa 2 **High** (trần lần sai MFA theo tài khoản **20/24h → 429** — ⚠️ Khanh xác nhận con số; bucket login lách được bằng khoảng trắng — lỗi từ IAM-001) + niêm phong payload challenge (ai ghi được Redis cũng không dựng được challenge) + precheck tài khoản trước khi tiêu mã + audit/IP + Sentry không còn gửi body request (lỗ từ IAM-001: password/refresh token). Số đo: **310/310** test hạ tầng thật bằng role app; turbo 35/35; smoke API build thật 23/23; 11 mutation đều bị bắt; Dart client khớp OpenAPI. ⚠️ **Trước deploy**: đặt `MFA_ENCRYPTION_KEY` trên Render (không có → API không khởi động), migrate + `db:app-role`; sau deploy owner/admin phải đăng nhập lại + enroll ngay (guide §13). Follow-up: cấp lại backup code / reset MFA / xoá `mfa-fail:*` khi reset mật khẩu → IAM-005. | AI Agent |
| 18/09/2026 | **TASK-IAM-003 `.1`–`.9` code xong, chờ Khanh commit + CI.** Khanh chốt Q1–Q4 theo khuyến nghị: (Q1) mở rộng role = **permission trên enum** — guard chỉ hỏi quyền, bảng role→quyền một file (`iam/role/permissions.ts`), thêm role = enum + 1 dòng (TypeScript + test bất biến bắt thiếu sót); (Q2) tenant lấy từ JWT theo API §7.3 `/operator/*`, khớp `:operatorSlug`/`:operatorId` khi route có (ghi chú ADR-017 v0.26); (Q3) **tách role Postgres**: app chạy bằng `vexenhanh_app` (không superuser/BYPASSRLS), migrate/seed bằng owner qua `MIGRATION_DATABASE_URL` — trước đó app chạy bằng SUPERUSER nên RLS vô hiệu hoàn toàn; (Q4) ngữ cảnh RLS tường minh `withScope(DbScope)` qua GUC `app.scope`, không ngữ cảnh → 0 row. RLS ENABLE+FORCE trên `operator_accounts`, `employee_accounts`, `auth_sessions` (tenant chỉ đọc), `operator_profiles` (đọc công khai, platform ghi); FK account → hồ sơ đổi RESTRICT. `@Authorize(permission)` = AccessTokenGuard → PermissionGuard (403 `PERMISSION_DENIED`) → TenantGuard (403 `TENANT_SCOPE_VIOLATION`). Review 2 agent: không finding critical/high; đã sửa 10 finding (DbScope có brand + ESLint rào ngữ cảnh, leo quyền qua ghi `auth_sessions`, cascade FK vượt RLS, production từ chối khởi động khi RLS vô hiệu, role/scope token, script role idempotent trên managed PG + SCRAM). **Workflow CI mới `db-integration.yml`** chạy test DB thật bằng role app (trước đó test tenant-RLS bắt buộc chưa từng chạy ở CI). Số đo: 248/248 test bằng role app; mô phỏng job CI trên DB mới 248/248; turbo xanh; smoke 11/11. ⚠️ **Production phải làm bước ops trước khi deploy** (guide §7): migrate → `db:app-role` → đổi `DATABASE_URL` Render sang role app — không thì API từ chối khởi động. Follow-up: slug bản sao ở `operator_accounts` (IAM-005), quyền PLATFORM_SUPPORT + 3 role Employee là giả định, scope `public` cho Guest (MKT). | AI Agent |
| 17/09/2026 | **TASK-IAM-002 `.3`–`.10` code xong, chờ Khanh commit + CI.** Hybrid token chạy thật: 4 đường login trả cặp token (JWT có `sid`), `POST /auth/refresh` (rotation + family), `/auth/logout` (idempotent, giết cả access token còn hạn qua `session:revoked:{sid}`), `/auth/re-auth` (bằng chứng `reauth:{sid}` 5 phút); audit Mongo best-effort không chờ; cron `session-cleanup` 03:00 giờ VN trên queue `session-maintenance`. **Số đo**: API 202/202 test (Postgres/Redis/Mongo thật), turbo 36/36, smoke theo `IAM-002-guide.md` 41/41, contract check Dart xanh, `mobile_shared` 30/30. **`code-reviewer` + `security-auditor` đã chạy, mọi finding đã sửa kèm test**: race revoke-family vs rotate đang commit (tái hiện được, sửa bằng UPDATE lặp + Redis trước Postgres), refresh không giới hạn khi thiếu IP (thêm bucket theo family, IPv6 gom /64), phiên Better Auth sống sót sau logout (xoá ngay sau khi đổi phiên), bucket re-auth khoá được từ cổng login, thiếu `Cache-Control: no-store`, JWT thiếu `exp` vẫn qua, audit chờ Mongo sau khi token cũ đã tiêu, kiểm account sau commit rotate. **Hai lỗi Redis lộ ra khi chạy thật** (có từ IAM-001): Redis chết thì request **treo mãi** thay vì 503; Redis sống lại thì API **503 mãi** tới khi restart — sửa bằng 3 option ioredis cho client đường request (BullMQ giữ nguyên). `redis.constants.ts` bỏ theo yêu cầu Khanh. ⚠️ Máy dev: `localhost:6379` là Redis trong WSL, không phải container. **Cần Khanh quyết**: CI chưa có service DB nên `*.int.spec.ts` không được CI gác; refresh hết hạn kiểu trượt (không trần tuyệt đối); 3 mã `AUTH_*_RATE_LIMITED` chưa vào LLD §7/GLOSSARY. Chi tiết 14 ghi nhận: `IAM-002-todo.md`. | AI Agent |
| 16/09/2026 | **IAM-002.2 — chốt schema `AuthSession`, 4 chỗ lệch bảng thiết kế + cách làm `user_ref`**. **(1) Thêm index `family_id` đứng riêng** — DB §7 chỉ có `(user_ref, family_id)`, nhưng revoke-family tra theo `family_id` một mình nên index composite (có `user_ref` đứng đầu) không phục vụ được; thiếu nó là **seq-scan cả bảng đúng lúc đang bị tấn công**. Đây là chỗ duy nhất chạm doc Approved ⇒ **`04-database-design` lên v0.5** (§7 + §1.2), thêm luôn index `operator_id` chuẩn bị RLS ở IAM-003. **(2) `revoked_reason` dùng enum thay `text`** — DB §7 không quy định kiểu cột, chỉ quy định index, nên không nghịch doc nào; tập giá trị đóng thì để Postgres chặn. **(3) `replaced_by_id` `@unique`, không self-relation** — rows không bao giờ xoá nên FK vô ích; unique bắt đúng bug hai row cùng trỏ một kế nhiệm. **(4) Bỏ `created_at`** — `issued_at` đã là thời điểm tạo, thêm nữa là hai nguồn cho một sự thật; vẫn giữ `updated_at`. **Và một chỗ lệch so với *chữ* của Q2**: `user_ref` **không** phải generated column — Prisma không mô hình hoá được, mà thêm bằng SQL thô rồi không khai trong `schema.prisma` thì lần `migrate dev` sau coi là drift và sinh `DROP COLUMN`. Thay bằng cột thường + một `CHECK` constraint đặt trong migration (nối `lower(subject_type)` + `:` + `subject_id`): Prisma **không** diff CHECK nên không xoá nó, khác hẳn cột — tinh thần Q2 vẫn được Postgres bảo đảm. Cập nhật: `04-database-design` §7 + §1.2 (v0.5), `IAM-002-todo` bảng thiết kế + khối ⚠️ 4 điểm, `IAM-002-verification-checklist` PHẦN B (thêm 2 mục kiểm index `family_id` và CHECK — kiểm bằng `\d auth_sessions` chứ không tin mỗi `schema.prisma`). | AI Agent |
| 16/09/2026 | **TASK-IAM-002: chốt 5 quyết định Q1–Q5** (Khanh chốt **cả năm theo khuyến nghị**) — gỡ chặn `.1`, sang `.2` được. **Q1 (a)** refresh token trả trong **body JSON** cho cả Web lẫn Mobile, cookie httpOnly + CSRF **defer** tới khi dựng app web đầu tiên (`apps/marketplace`/`operator-os`/`admin` còn rỗng). **Q2 (b)** `auth_sessions` dùng **2 cột `subject_type` + `subject_id`** cộng `user_ref` sinh ra để giữ index `(user_ref, family_id)` của DB §7; `subject_type` có **4 giá trị** (`passenger`/`operator`/`employee`/`platform`) chứ không phải 3 `scope` của JWT — nếu chỉ lưu scope thì revoke-all sẽ đá nhầm người vì scope `operator` gộp cả `operator_accounts` lẫn `employee_accounts`. **Q3 (a)** `/auth/re-auth` chỉ **cấp bằng chứng** (Redis `reauth:{sid}` TTL 5 phút); guard `@RequireReauth()` để task tiêu thụ đầu tiên làm, vì endpoint nhạy cảm (refund/payout/đổi bank account) chưa tồn tại. **Q4 (a)** FR-IAM-15 (danh sách phiên + thu hồi theo thiết bị) **chuyển sang TASK-IAM-005** — API §7.1 chưa có endpoint, thêm là mở rộng phạm vi; v1 **không giới hạn** số phiên hoạt động. **Q5 (a)** giữ **strict** đúng ADR-017 (không cửa sổ ân hạn), đổi lại **client bắt buộc single-flight** khi refresh. **Ba nghĩa vụ kéo theo đã xử lý**: `07-security` §5.1 hàng _Token storage_ ghi chú defer cookie Web (doc hết lệch code); `11-project-task-breakdown` dòng TASK-IAM-005 nhận thêm FR-IAM-15; `FND-009-todo` nhận thêm follow-up single-flight cho `AuthInterceptor`. Phía Web chưa ghi được vì **chưa có task nào dựng app web** — phải nhớ khi task đó ra đời. Checklist IAM-002 PHẦN A tick 2 mục đầu + DoD `.1`. | AI Agent |
| 15/09/2026 | **Vá 3 lỗi sau khi đóng FND-009** (Khanh phát hiện, cả ba đều tái hiện được). **(1) `parseVnd` nhận chuỗi sai thành số tiền khác**: bỏ dấu chấm TRƯỚC khi xác thực, và `int.parse` không truyền radix ⇒ `'1.5'`→`15`, `'1.2.3'`→`123`, `'0xffffffffffffffff'`→`-1` (cuộn vòng im lặng) — trái đúng hợp đồng `FormatException` ghi trong doc của chính hàm. Sửa: xác thực bằng regex (dấu âm tuỳ chọn, rồi dãy số trần *hoặc* dãy nhóm đúng ba) **trước** khi bỏ dấu nhóm, rồi `int.parse` với `radix: 10`. **(2) Nhánh fallback của `toApiFailure` vẫn ném `TypeError`**: ép `data['status'] as int?` và `data['requestId'] as String?` mà không kiểm kiểu — body có `status: '401'` là ném. Nặng hơn: lỗi ném **từ trong** một nhánh `catch` KHÔNG được nhánh `catch` anh em bắt (đo được `caughtBySibling=false, escaped=true, finallyRan=true`), nên nó thoát khỏi `_run`, `finally` vẫn tắt `_busy`, và màn hình im lặng — tức vô hiệu hoá đúng bản vá vừa làm trước đó. Sửa: kiểm `is int` / `is String`, lấy status từ HTTP response, và tách việc dịch lỗi ra `_describeDioError` (có try/catch riêng) để không bao giờ ném từ trong `catch`. **(3) CI không gác hợp đồng API**: `ci.yml` chỉ chạy `pnpm gen:api-client` rồi vứt, không so sánh; `mobile.yml` không có `apps/api/**` trong `paths`; và **không có gì sinh lại Dart client** — đúng điểm ADR-028 đòi (*“chạy trong CI cạnh `openapi-typescript` cho Web”*). Thêm `git diff --exit-code` sau bước gen trong `ci.yml` (bắt lệch phía TS), và workflow mới **`contract.yml`** sinh lại Dart client bằng generator pin **v7.25.0** rồi so. **Bẫy gặp khi dựng**: `dart format` chọn style theo language version của package, nên format một bên mới là lệch ~3300 dòng vì lý do vô nghĩa; phải chép cả hai sang chỗ trung lập rồi format bằng **cùng một lệnh**, và loại `.g.dart` (do `build_runner` sinh, không phải generator). Đã chạy thử hai chiều: trạng thái hiện tại → xanh; cố tình chèn một class lạ → đỏ. **Đo lại**: `mobile_shared` **30/30** test pass, `flutter analyze` sạch cả 3 gói. Test hồi quy dùng đúng các ca Khanh đưa. | AI Agent |
| 15/09/2026 | **TASK-FND-009 → Done**. Nền Mobile Flutter dựng xong theo ADR-028: 2 app `passenger_mobile` + `employee_mobile` (`com.vexenhanh.*`), package Dart chung `packages/mobile_shared/` (HTTP client + `AuthInterceptor` Bearer/401 + parse RFC 7807 + wrapper `flutter_secure_storage` + tiện ích VND `BIGINT`), Dart client `packages/api_client_dart/` sinh bằng `openapi-generator` **v7.25.0** (`dart-dio`), pin SDK qua **`.fvmrc` 3.47.2** — CI đọc thẳng file này qua `flutter-version-file`, và workflow `mobile.yml` tách hẳn khỏi job Node. **Đường dọc chạy thật**: login Email-OTP trên **thiết bị Android thật** (Galaxy S23 Ultra, qua `adb reverse tcp:3000`) → 200 + `scope=passenger`, xác minh bằng row `users` sinh ra trong Postgres. **Verify ở tầng APK** (đọc merged manifest, không phải file nguồn): `passenger` **không** có `ACCESS_FINE/COARSE_LOCATION` — `tools:node="remove"` gỡ được quyền do AAR `org.maplibre.gl` tự khai; `employee` có; **cả hai không có `ACCESS_BACKGROUND_LOCATION`**; `usesCleartextTraffic` chỉ tồn tại ở `src/debug/`. **Số đo cuối**: `flutter analyze` sạch 3 gói · `flutter test` **26 + 1 + 1** pass · `dart analyze` `api_client_dart` sạch · `pnpm turbo run typecheck lint test build` **36/36** · build APK debug cả 2 app OK. **Review đóng task**: `code-reviewer` + `security-auditor` — **không finding Critical/High**. Đã vá ngay: `allowBackup="false"` (token blob không theo Auto Backup lên Drive, tránh `InvalidKeyException` sau restore), xoá `<service>` chết trỏ vào class không tồn tại trong manifest employee, `toApiFailure` đọc phòng thủ từng field thay vì all-or-nothing và không đẩy chuỗi tiếng Anh nội bộ của Dio ra UI, `_run` bắt thêm loại lỗi ngoài `DioException`, dọn `_otpController` sau khi verify. **Bác 2 finding sau khi tự kiểm**: (a) “YAML anchor làm hỏng workflow” — GitHub đã hỗ trợ anchor từ 18/09/2025, bật mặc định (chỉ merge key `<<:` là không); (b) “passenger không được xin `CAMERA`” — ADR-028 xếp Camera vào đúng profile tối thiểu của app hành khách. ⚠️ **`mobile.yml` chưa chạy thật lần nào** (chỉ trigger trên `develop`, việc làm trên branch `TASK-FND-009`) — toàn bộ lệnh đã chạy tay và pass, nhưng “CI xanh” chỉ chắc sau lần gộp đầu tiên. Follow-up có chủ (chi tiết ở `FND-009-todo.md`): signing config thật trước khi phát tán APK release (**TASK-OPS-001**), nối `onUnauthorized` + đăng xuất, giới hạn Bearer theo origin trước khi dùng presigned URL R2 / redirect thanh toán, và thêm assert merged manifest vào CI. | AI Agent |
| 15/09/2026 | **FND-009: thu hẹp `.2` + chấp nhận `api_client_dart` lệch bản sinh** (Khanh chốt). **(1) Hoãn “design token + trạng thái nền loading/error/empty/permission” sang task UI đầu tiên chạm `06-ui-ux-flow-specification` (TRN/BTP)**: FND-009 chưa có màn hình nghiệp vụ nào — `passenger_mobile` mới có một màn đăng nhập dựng để chứng minh chuỗi API, thiết kế token khi chưa biết màn hình cần gì là đoán. Ba mục còn lại của `.2` đã xong kèm test (HTTP wrapper + `AuthInterceptor` Bearer/401 + parse RFC 7807, wrapper `flutter_secure_storage`, tiện ích VND `BIGINT`). DoD Mobile `CLAUDE.md §6.3` không đổi, chỉ dời thời điểm. **(2) `packages/api_client_dart` lệch bản sinh**: commit `c693781` chạy `dart format` lên `lib/src/api/{auth,health}_api.dart` + gỡ 4 `import` thừa. Đối chiếu `git diff -w --ignore-blank-lines` → **0 thay đổi logic**, chỉ xuống dòng và bỏ import. Giữ nguyên thay vì revert; regen sẽ xoá chúng và trả lại 3 cảnh báo `unused_import` — checklist PHẦN B đã coi là chấp nhận được, **không sửa tay lại**. **Đo được cùng ngày**: `flutter analyze` sạch cả 3 gói Flutter; `flutter test` 19 + 1 + 1 pass; `dart analyze` `api_client_dart` sạch; `pnpm turbo run typecheck lint test build` **36/36**; pnpm workspace vẫn đúng 9 gói Node; build APK debug **cả 2 app** và đọc manifest đã gộp → `passenger` **không** còn `ACCESS_FINE/COARSE_LOCATION` (`tools:node="remove"` gỡ được AAR MapLibre), `employee` có; **cả hai đều không có `ACCESS_BACKGROUND_LOCATION`**. Cập nhật: `FND-009-todo` §.2/§.3, `FND-009-verification-checklist` PHẦN A + E. | AI Agent |
| 14/09/2026 | **Hoãn `flutter_background_geolocation` — không khai ở bước FND-009.5**. ADR-028 ánh xạ background geo sang plugin này, nhưng khi khảo sát để khai thì phát hiện nó là **plugin thương mại**: README + `help/INSTALL-ANDROID.md` của chính nó ghi _"The SDK is fully functional in `DEBUG` builds — no license required"_ và _"A license is required for `RELEASE` builds"_ (cả iOS lẫn Android) — trong khi file `LICENSE` ghi **Apache-2.0**, gây hiểu nhầm là miễn phí. Xác minh bằng cách cài thử vào `apps/employee_mobile`, đọc file trong `.pub-cache`, rồi revert `pubspec.yaml` + `flutter pub get`. **Quyết định**: FND-009 chỉ *khai báo* plugin, background geo mãi tới **TASK-EMP-001/002** mới chạy ⇒ chốt một dependency trả phí bây giờ là quyết định sớm không cần thiết. Bỏ khỏi danh sách khai ở `.5`; giữ `geolocator` (BSD-3, miễn phí). **3 phương án cân lại ở EMP-001**: (a) chỉ build `DEBUG` — hợp lệ vì v1 không lên store (ADR-028); (b) `flutter_foreground_task` + `geolocator` — miễn phí; (c) mua license. **Không đổi ADR-028** (quyết định vẫn đứng), chỉ thêm cảnh báo license + mốc chốt lại. Cập nhật: `CLAUDE.md` §2 Layer 7, ADR-028, FND-009 todo/guide/checklist. | AI Agent |
| 14/09/2026 | **Chốt quy ước đặt tên thư mục Flutter: `snake_case`** (Khanh chốt) — `apps/passenger_mobile`, `apps/employee_mobile`; `packages/mobile_shared`, `packages/api_client_dart`. Lý do: tên package Dart bắt buộc `snake_case`, để thư mục trùng tên package thì `flutter create` gọi thẳng được, bỏ hẳn bẫy `--project-name` (thư mục kebab làm `flutter create` fail). Đây là **ngoại lệ** duy nhất so với kebab-case của workspace Node (`apps/operator-os`, `packages/api-client`) — ghi vào `DOMAIN-MAP §3`. Cascade: SRS/HLD/LLD/DB/UI/Deploy/ADR-028/Task, DOMAIN-MAP, CLAUDE/AGENTS, README EN-VI, `pnpm-workspace.yaml`, `.github/instructions`, 3 doc FND-009. ⚠️ **Lỗi chức năng đã sửa kèm**: `.dockerignore` dùng glob `apps/*-mobile` nên sau khi đổi tên không còn khớp — thư mục Flutter sẽ lọt vào Docker build context; đổi thành `apps/*_mobile` (CI path filter trong ADR-028 + guide FND-009 cũng vậy). **Cạm bẫy Gradle phát hiện cùng ngày**: Flutter sinh `android/settings.gradle.kts` không khai `rootProject.name` nên mọi app đều mang tên thư mục `android` → nạp 2 app trong một IDE/Gradle workspace báo `A project with the name android already exists`; đã khai tên riêng cho từng app và verify bằng `gradlew projects`. | AI Agent |
| 14/09/2026 | **TASK-IAM-001 → Done**. Login 3 namespace chạy thật: Passenger (Email-OTP), Operator/Employee (`{slug}/{username}`), Platform (`platform/{username}`); Better Auth handler mount ở `/api/auth/*`. **Smoke test trên Docker local 14/09**: platform/operator login 200 kèm claim tenant; sai password / sai namespace 401; email rác 400; `oauth/twitter` + `oauth/facebook` 400 (v1 chỉ Google); otp request 200 → verify 200 `scope=passenger`; `/api/auth/get-session` 200 và `/api/auth/callback/google` 302 (**trước khi mount là 404**). **127 test / 20 file**, typecheck + lint + test + build **36/36**. Hai review bắt buộc (`code-reviewer` + `security-auditor`) đã xong, toàn bộ finding chặn task đã sửa: rate limit login theo identifier + IP, `RESEND_API_KEY` fail-fast production, seed chặn production + bỏ mật khẩu mặc định, đối chiếu `operatorId` nhánh owner (cách ly tenant), allowlist `callbackURL` chống open redirect, scrypt `N=2^16`. **Track (không chặn Done)**: (a) `IAM-001-verify-guide.md` **chưa cập nhật** — còn ghi 67 test, mật khẩu `ChangeMe!*` đã bị xoá khỏi seed, và bước đọc OTP ở log không còn đúng (console notifier không in OTP nữa; dev phải đọc từ bảng `verifications`, hoặc Resend trả 422 vì domain gửi chưa verify); (b) `register()` chưa có test riêng, chưa có test hợp đồng mức HTTP cho 6 endpoint (CR-C4) — `proxy-auth.integration.spec.ts` mới phủ phần proxy/rate-limit; (c) Swagger `/v1/docs` vẫn mở không điều kiện kể cả production (thuộc FND-008/hardening).                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | AI Agent |
| 12/09/2026 | Đồng bộ Security §4/§7/§8 với SRS UC-08: Guest được yêu cầu hủy vé / hoàn tiền cho booking / ticket đã xác minh, theo policy; xác minh bổ sung nếu policy yêu cầu. Giữ nguyên trạng thái tài liệu.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | AI Agent |
| 08/09/2026 | **Đổi Mobile framework Expo / React Native → Flutter (ADR-028, supersedes ADR-014)**. Khanh chốt 08/09/2026. **Giữ nguyên 2-app split** (`passenger_mobile` + `employee_mobile`) và toàn bộ framing actor / permission / store listing của ADR-014; chỉ đổi framework. Blast radius verify trước khi làm: 2 app mobile mới là scaffold (2 file / ~30 dòng mỗi app), `packages/ui-mobile-shared` 6 dòng → **0 dòng code nghiệp vụ Mobile bị bỏ**; `apps/api` không đổi. **Kéo theo amend 8 ADR**: ADR-009 (single-language thu hẹp còn BE+FE Web — Dart không share `packages/types`), ADR-012/013 (Dart client `openapi-generator` dart-dio; Flutter ngoài pnpm workspace/Turborepo), ADR-017 (`flutter_secure_storage`), ADR-020 (push **Expo Push → FCM + APNs trực tiếp**), ADR-023/026 (**EAS → Codemagic**; `sentry_flutter`), ADR-025 (Maestro giữ + `flutter_test`/`integration_test`), ADR-027 (`maplibre_gl`). **Cascade**: SDLC 01–09/11/12 (58 sửa) + DOMAIN-MAP + PROJECT-STATE + AGENTS/CLAUDE + README EN/VI + config (.gitignore/.dockerignore/turbo/eslint/tsconfig.base/vscode/pnpm-workspace). **Xoá**: `apps/passenger_mobile`, `apps/employee_mobile`, `packages/ui-mobile-shared`, `packages/config/tsconfig/react-native.json`. **3 điểm mở đã đóng cùng ngày (Khanh)**: (a) **lý do chốt = phục vụ môn học** — ràng buộc học thuật, không phải lập luận kỹ thuật; 4 lý do ADR-014 loại Flutter vẫn đúng, chỉ bị ghi đè (ghi rõ trong ADR để không hiểu nhầm). (b) **Build**: Android là nền tảng chính (build local trên Windows), iOS tuỳ chọn khi có máy macOS — _thiết bị iOS không thay được Mac, chỉ chạy được bản đã build_. (c) **OTA bỏ qua** vì **v1 không phát hành store** → không có store review để né. ⇒ Hệ quả tiêu cực (1) hạ từ blocker xuống rủi ro thấp, (2) thành N/A. ⚠️ **Cả hai sống lại nếu sản phẩm lên store về sau**. **Track**: ~~verify `openapi-generator` với OpenAPI 3.1~~ → **đóng 09/09/2026** (xem entry cùng ngày); Flutter project chưa tạo. | AI Agent |
| 06/06/2026 | **Harden FND-007 audit append-only gap**. App-layer guard giờ chặn thêm `bulkWrite` và `insertMany` trên `audit_event`/`system_log` để tránh bypass Mongoose update/delete hook; regression test tăng API suite lên **33 tests**. Thêm script `mongo:audit:hardening` tạo collection/index bằng admin URI rồi tạo Mongo role/user app chỉ có `find` + `insert` trên `audit_event` và `system_log`; production `MongoAuditModule` tắt `autoCreate/autoIndex` để app user không cần quyền DDL. `.env.example` thêm biến hardening production. **Verify**: API typecheck + test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | AI Agent |
| 06/06/2026 | **Implement TASK-FND-008 OpenAPI + api-client foundation**. Thêm OpenAPI 3.1 auto-gen cho NestJS bằng `@nestjs/swagger` + `nestjs-zod`: health endpoints dùng Zod DTO từ `@vexenhanh/types`, runtime expose `/v1/docs` + `/v1/openapi.json`, và có `OpenApiModule` scan-only để generate spec không cần Postgres/Mongo/Redis. `packages/api-client` chạy `openapi-typescript` sinh `src/generated/openapi.json` + `schema.ts`, export `paths/components/operations/webhooks`; root CI step `pnpm gen:api-client` giờ chạy thật. `@vexenhanh/types` build CJS để API CommonJS import schema runtime an toàn. **Verify**: `pnpm gen:api-client`; API typecheck + **31 tests** + lint + build; package `types`/`api-client` typecheck/build. FND-008 → Done.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | AI Agent |
| 06/06/2026 | **Implement TASK-FND-007 audit base + DB-driver boundary**. Thêm `audit/` module với Mongoose schemas strict cho Mongo collections **`audit_event`** và **`system_log`** (`schemaVersion`, `createdAt`, indexes, time-series option), `AuditService.recordAuditEvent/recordSystemLog` append-only và tự gắn `requestId`/`traceId` từ FND-006. App-layer append-only guard chặn update/delete/replace; `MongoAuditModule` dùng typed `APP_CONFIG` và fail-fast `MONGODB_AUDIT_URI`; API + Worker import `AuditModule`. ESLint flat-config thêm local rule cấm Mongoose ngoài `audit/` + `database/`, cấm Prisma driver/client ngoài `database/` (generated client excluded). Tests: service append shape, strict schema, append-only mutation block, boundary lint rule. **Verify sạch**: API typecheck + **30 tests** + lint + build; root typecheck/lint/test/build **12/12**. FND-007 → Done. **Track**: production append-only hardening vẫn cần Mongo user permission/role không có update/delete theo ADR-011/DB-PRIN-04.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | AI Agent |
| 04/06/2026 | **Implement TASK-FND-006 observability foundation**. API + Worker dùng **Pino structured logger** thay console/Nest default; request middleware phát/giữ `x-request-id`; global filter trả **RFC 7807** `application/problem+json` với `code` + `requestId` + `traceId`; Bull Board guard align problem response; Sentry Nest SDK init sớm qua `instrument-api`/`instrument-worker` khi có `SENTRY_DSN`; OTel API helper expose tracer/trace id; env thêm `LOG_LEVEL`, `SENTRY_TRACES_SAMPLE_RATE`, `OTEL_SERVICE_NAME`. Thêm regression test request-id/problem-details/env/Bull Board. **Verify sạch**: API typecheck + 22 test + lint + build; root typecheck/lint/test/build **12/12**. FND-006 → Done. **Track**: set Sentry DSN/source maps khi deploy staging; collector/OTLP exporter defer tới khi có observability backend riêng.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | AI Agent |
| 03/06/2026 | **Nâng Prisma 5.22 → 7.8 (driver adapter) + amend ADR-011** (Khanh chốt — đồng bộ + xoá workaround). Prisma 7: `datasource` bỏ `url` → **`@prisma/adapter-pg` + `pg`** ở PrismaClient + **`prisma.config.ts`** (schema + adapter + tự load env 2-part) cho Migrate/Studio. **Lợi ích ròng**: (1) **bỏ query-engine binary** → Dockerfile gỡ libssl COPY + openssl + binaryTargets (xoá bẫy distroless đã vá tay trước đó); (2) `prisma.config` hợp nhất env → **bỏ `dotenv-cli`** + un-wrap prisma scripts; (3) editor v7 hết gạch đỏ. `PrismaService` inject APP_CONFIG → adapter (DATABASE_URL required tại điểm dùng). Migration `init` giữ nguyên (SQL compat). amend ADR-011 (Prisma 5→7, blast radius nhỏ vì foundation). **Verify (Khanh chạy — phần dễ treo)**: `pnpm install` → typecheck → validate → migrate status → app boot + 4 health 200; docker build verify sau.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | AI Agent |
| 03/06/2026 | **Provision DB/cache cloud + wire + migration đầu tiên (kết nối THẬT cả 3)**. Provision free SG: **Supabase** (Postgres, Session pooler IPv4 — tránh direct IPv6) + **Atlas M0** (Mongo audit, network 0.0.0.0/0 cho Render IP động) + **Upstash** (Redis `rediss://` TLS) → URL ở `.env.production` + Render secrets (`sync:false`, đã thêm key vào render.yaml). Hạ tầng dev local: `docker-compose` (PG/Mongo/Redis + pgAdmin/mongo-express; fix email pgAdmin `.local`→`.com`). Wire **DatabaseModule + MongoAuditModule** vào AppModule; thêm `DatabaseHealthService`/`MongoHealthService` + endpoint `/v1/health/{postgres,mongo,redis,queues}` (RFC 7807, 503 nếu down). Model Prisma đầu **`HealthCheck`** (non-tenant) + migration `20260603163757_init` (bảng `health_checks`) apply local OK. Prisma scripts bọc `dotenv-cli` chọn `.env.{env}` (do 2-part env). **Verify**: migrate apply + 4 health endpoint = 200 (Postgres/Mongo/Redis connect thật) + typecheck/13-test/build xanh. FND-003 → Done. **Track**: `migrate deploy` lên Supabase khi deploy Render + set Render secrets trước; RLS policy thật áp khi có tenant table đầu (domain task).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | AI Agent |
| 03/06/2026 | **Tách env 2 phần (FND-005 refine)** (Khanh yêu cầu dễ quản lý). `apps/api/.env` = **chỉ selector `NODE_ENV`**; giá trị tách sang **`.env.development` / `.env.production`**. `loadAppConfig` nạp `.env` → đọc NODE_ENV → nạp `.env.{NODE_ENV}` (Node 24 `process.loadEnvFile`). **Precedence: shell/Render > `.env` > `.env.{NODE_ENV}`** (loadEnvFile KHÔNG override biến đã set — verified, Render-safe). **Bug-fix qua functional smoke**: chuỗi rỗng `SENTRY_DSN=""` ≠ undefined → fail `.min(1)`; thêm `preprocess` coi `""`/whitespace = unset (+ test regression). `.env.example` thành template 2-phần. Verify: typecheck + 13 test + build + smoke dev/prod ✓. (`.env.*` đã gitignore; `.env.example` commit.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | AI Agent |
| 03/06/2026 | **Review TASK-FND-004 (Redis/BullMQ) + implement TASK-FND-005 (env config) + hoàn tất Fix 3 distroless**. **FND-004 (agent) chất lượng cao**: ioredis + BullMQ + Bull Board; processor CHỈ ở WorkerModule (API enqueue / worker process — đúng ADR-024); Bull Board guard **fail-closed production** + RFC 7807 + 7 test; redis options chuẩn BullMQ (`maxRetriesPerRequest:null`, lazyConnect boot-safe). **FND-005 (tôi làm)**: Zod `envSchema` + `loadAppConfig` (Node 24 `process.loadEnvFile`, **0 dep mới**) + module global `APP_CONFIG` typed + **fail-fast**; nạp `.env` (vá lỗ hổng app không đọc env runtime) + validate lúc boot; wire AppModule/WorkerModule (đầu imports) + main/worker dùng config; `.env.example` đủ biến; 5 test. **Distroless Fix 3 done**: COPY `libssl.so.3`/`libcrypto.so.3` build→runtime distroless (engine deb12 chạy deb13 OK — glibc forward-compat). **Verify sạch**: typecheck + **12 test** + lint + build + root turbo 12/12. FND-004/005 → Done. **Track**: wire DatabaseModule + provision dịch vụ free SG (Postgres/Mongo/Redis) trước khi app chạy thật; siết `DATABASE_URL`/`REDIS_URL` thành `required` khi wire.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | AI Agent |
| 03/06/2026 | **Review TASK-FND-003 (DB foundation) + sửa lệch design RLS-key**. Lớp DB agent dựng OK (Prisma 5.22 + Mongoose 8.24 Hybrid-A tách đúng; `PrismaService.withOperatorContext` set GUC transaction-local). **Sửa lệch design**: RLS-key `operator_slug → operator_id` (impl khớp DOMAIN-MAP §6 + 04-DB hơn ADR-017) ở HLD §11 / 04 DB-PRIN-01 / 07 §6 / ADR-017; thêm `operatorId` vào JWT payload để set GUC khỏi lookup (`operatorSlug` vẫn cho TenantGuard match URL + namespace login). **Bổ sung**: convention RLS table (ENABLE + **FORCE** RLS + policy + **app role KHÔNG owner/superuser** kẻo bypass) vào DB-PRIN-01 + comment PrismaService; script `prisma:migrate:dev/deploy`. **Track (treo)**: bẫy distroless×Prisma (binaryTargets + `prisma generate` trong Docker build + libssl — đã guide), wire DB vào AppModule khi có DATABASE_URL, mandatory tenant-RLS test khi có table, chuẩn hoá env `MONGODB_AUDIT_URI` ở FND-005. Bump Ngày cập nhật 02/04/07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | AI Agent |
| 03/06/2026 | **Amend runtime ADR-009: Node.js LTS 22.x → 24.x** (Khanh yêu cầu nâng runtime). Code config: root `package.json` đổi `engines.node` `">=24 <25"` và `@types/node` `^24.12.4`; `pnpm-lock.yaml` regenerate. Docs hiện hành đồng bộ: AGENTS/CLAUDE, README EN/VI, 09 Deploy, ADR-009 title/body/index + ADR-023 Docker mapping. Dependency verify: npm registry engine metadata của direct deps không có package chặn Node 24; lưu ý verify command local chạy trên Node 22.22.3 sẽ báo unsupported engine cho repo sau khi đổi policy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | AI Agent |
| 02/06/2026 | **Bỏ version-pinning (Node + doc) — git là nguồn lịch sử** (Khanh chốt 2 quyết định: giảm file nhiễu root + churn/cascade doc). (1) **Node**: xóa `.nvmrc` (thừa — `.node-version`=22 + `package.json engines` `">=22 <23"` + `.npmrc` đã đủ; fnm/asdf/Volta đều đọc `.node-version`, engines = nguồn chuẩn CI/pnpm); sửa 2 README bỏ nhắc `.nvmrc`. (2) **Doc version**: bỏ trường số `Phiên bản vMAJOR.MINOR` khỏi metadata 13 doc SDLC + cột **Version** khỏi PROJECT-STATE §1 → định danh doc = **Trạng thái** (gate Draft/Review/Approved) + **Ngày cập nhật**. Cập nhật chuẩn **00a §4.2** (bỏ Phiên bản bắt buộc, thêm Ngày cập nhật, fix Dự án→Marketplace-Ve-Xe-Nhanh) + **§4.3** (bỏ bảng revision bắt buộc + rule "tăng phiên bản" → git-based; §1.2 cũ giữ làm historical, không thêm dòng). Gỡ **47 citation `(vX.Y)`** + bare ref ("SRS v1.20" DOMAIN-MAP; 11-task mermaid/TASK-DOC `v0.x`) ở SDLC/DOMAIN-MAP/AGENTS/CLAUDE. Giữ: §1.2 revision table cũ (historical) + 12 Release Notes product-version SemVer (hợp lệ, khác doc-metadata-version). §7 dọn 5 entry trùng đã có trong `archive/CHANGELOG.md` (10→6). **Hết churn/cascade/drift version-ref.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | AI Agent |
| 02/06/2026 | **Đổi Routing/Map: Mapbox → Goong (ADR-027 supersede ADR-021)** — Khanh phát hiện **Mapbox KHÔNG dùng được tại VN** (chủ quyền Hoàng Sa/Trường Sa; research: Grab phạt 60M VND dùng OSM 4/2023, Ninja Van gỡ bản đồ Mapbox 5/2023). Workshop 3 option (Goong / VietMap / OSM-OSRM); Khanh chốt **Goong** (Make-in-VN compliant + Mapbox/Google-compatible swap thấp nhất + free tier + geocode VN tốt hơn). Giữ framing assist-level + adapter `RoutingProvider` + cache-once DB; `external/routing/mapbox/` → `goong/`; Web `@goongmaps/goong-js` (Mapbox-GL-compat), Mobile MapLibre RN. **Chưa có code Mapbox** (apps mới stub) → thuần rework tài liệu. File 10 **v0.21 → v0.22**: thêm **ADR-027**, mark **ADR-021 Superseded**. Cập nhật Mapbox→Goong + ADR-021→ADR-027 (sed + hand): HLD/LLD/DB/UI/Deploy/Task/DOMAIN-MAP + CLAUDE.md §2 + AGENTS.md (không bump version từng doc — coordinated rename Draft). OSRM escape-hatch giờ cần VN-corrected data (ưu tiên VietMap escape #2). Thêm **compliance constraint**: bản đồ phải hiển thị Hoàng Sa/Trường Sa (luật bản đồ VN).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | AI Agent |
| 02/06/2026 | **Flatten doc structure + doc-reader subagent** (Khanh yêu cầu). (1) Bỏ wrapper `vi/` (thừa khi chỉ 1 ngôn ngữ sau khi gỡ en/): `doc/vi/SDLC/*` → **`doc/SDLC/`** (00-12); sed cập nhật ~22 path ref (CLAUDE.md / AGENTS.md / DOMAIN-MAP / GLOSSARY / PROJECT-STATE §1). SDLC docs cross-ref nhau bằng bare filename → flatten không gãy. (2) Bỏ `vi/agent/`: authoring standard → archive `agent-authoring-standard.md` (giữ ISO 15289/29148 + B3); gỡ row 00b §1 + 3 §6 historical row vi/agent. (3) AGENT.md rewrite slim (~32 dòng, doc/ guide, không vi/en); AGENTS.md line 5 sửa (CLAUDE.md = coding brain). (4) Tạo subagent **`sdlc-doc-reader`** (`.claude/agents/`, read-only Read/Grep/Glob, sonnet) — đọc SDLC docs trả design summary có citation, giữ context coding agent lean. Cấu trúc cuối: `doc/{AGENT.md, SDLC/00-12, context/{DOMAIN-MAP, GLOSSARY, PROJECT-STATE, archive/}}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | AI Agent |
| 02/06/2026 | **Pivot CLAUDE.md → coding agent + tích hợp 2 repo** (Khanh yêu cầu). (1) **CLAUDE.md** đổi vai trò doc-Scrum-Master → **coding agent**: §1 role + Karpathy 4 nguyên tắc, §3 thêm `.claude/agents` + `.claude/skills`, §4 constraints (gỡ "no-code" → code rules: ADR/DOMAIN-MAP/money-BIGINT/adapter/test/git), §5 coding workflow (think→surgical→goal-driven→subagent→review), §6 code-sprint DoD + build-phase status, §7 context. Giữ §2 stack + §8 verbatim (surgical). (2) **`.claude/agents/`** = 15 subagent curated (VoltAgent MIT) khớp stack: typescript-pro/nextjs-developer/react-specialist/backend-developer/fullstack-developer/api-designer/postgres-pro/docker-expert/devops-engineer/mobile-developer/code-reviewer/qa-expert/security-auditor/performance-engineer/agent-organizer. (3) **`.claude/skills/karpathy-guidelines/`** = Karpathy 4 principles (multica-ai MIT) + nhúng vào AGENTS.md. Kiến trúc: AGENTS.md = WHAT (stack/style), CLAUDE.md = HOW (Claude Code role/subagents/workflow).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | AI Agent |
| 02/06/2026 | **Dọn cấu trúc doc — gỡ `doc/en/`** (Khanh duyệt). `en/SDLC` rỗng + `en/agent/00` = bản dịch song song không consumer (vi phạm chính nguyên tắc AGENT.md "no parallel translation unless requested"). Xóa toàn bộ `doc/en/`; giữ `vi/agent/00` (standard đang dùng — ISO 15289/29148 + B3). AGENT.md rewrite Vietnamese-primary (gỡ nhánh vi/en trong 4 task-flow + directory map; 106 → ~80 dòng; thêm `archive/` + `AGENTS.md` ref). Gỡ row 00b-EN khỏi §1; gỡ row en/agent khỏi CLAUDE.md §3 map. Đóng §6 known-issue (abridged SDLC → `AGENTS.md`). Cấu trúc còn: `doc/{AGENT.md, context/{…,archive/}, vi/{agent/00, SDLC/00-12}}` — 1 nguồn, không trùng lặp.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | AI Agent |
| 02/06/2026 | **Context-engineering restructure** (Khanh yêu cầu — PROJECT-STATE quá dài; tham khảo chuẩn AGENTS.md + Claude Code lean-context). §7 changelog 46 entry → `archive/CHANGELOG.md` (giữ ~5 mới nhất); §3/§4 nén prose re-close OQ → terse + pointer ADR. **PROJECT-STATE 72KB → 20KB (−72%)**. CLAUDE.md Sprint 0-4 tables + roadmap §6.5.1/2 → `archive/SPRINT-LOG.md`; **321 → ~200 dòng**. Tạo root **`AGENTS.md`** (coding-agent brief cross-tool). Policy mới: lịch sử chi tiết → **git commit**, §7 = ~5 entry cửa sổ trượt (CLAUDE.md §5 step3 + §6.6 updated). Always-loaded context ~95KB → ~25KB (−70%) → coding agent đỡ tốn budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | AI Agent |

> **Lịch sử đầy đủ** (entry trước 02/06/2026): `archive/CHANGELOG.md`. Từ 02/06/2026: thay đổi chi tiết ghi vào **git commit message**; §7 chỉ giữ ~5 entry gần nhất (cửa sổ trượt).
